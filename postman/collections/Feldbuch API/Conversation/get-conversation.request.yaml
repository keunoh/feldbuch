$kind: http-request
name: Get Conversation
method: GET
url: '{{baseUrl}}/api/conversations/{{conversationId}}'
order: 2000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{accessToken}}'
pathVariables:
  - key: conversationId
    value: '{{conversationId}}'
