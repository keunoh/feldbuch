$kind: http-request
name: Summarize Job Status
method: GET
url: '{{baseUrl}}/api/ai/jobs/{{jobId}}'
order: 2000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{accessToken}}'
pathVariables:
  - key: jobId
    value: '{{jobId}}'
