$kind: http-request
name: Delete Note
method: DELETE
url: '{{baseUrl}}/api/notes/{{noteId}}'
order: 5000
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{accessToken}}'
pathVariables:
  - key: noteId
    value: '{{noteId}}'
