$kind: http-request
name: logout
description: |-
  로그아웃하고, token을 삭제합니다.

  **인증**: Bearer Token 필요

  **Response** `200 OK`
  ```json
  {
    "success": true,
    "data": null,
    "error": null
  }
  ```
url: "{{baseUrl}}/api/auth/logout"
method: POST
auth:
  type: bearer
  credentials:
    token: "{{accessToken}}"
order: 3417821220438738
