spring:

  application:
    name: feldbuch

  profiles:
    active: local

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true

openai:
  base-url: https://api.openai.com/v1
  model: gpt-4.1-nano

logging:
  leel:
    org.hibernate.SQL: debug
